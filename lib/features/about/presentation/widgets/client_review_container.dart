import 'package:flutter/material.dart';

class ClientReviewContainer extends StatelessWidget {
  const ClientReviewContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
